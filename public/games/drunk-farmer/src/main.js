// Intentionally empty.
// The Vite starter scaffold (hero, counter, docs links, style.css) was removed.
// The game canvas is mounted directly from index.html:
//   initCanvas('#game') -> createGrid() -> renderGrid(...)
